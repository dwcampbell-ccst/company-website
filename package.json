{
  "name": "company-website",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "api:dev": "node api/dev-server.js",
    "build": "cd frontend && npm run build",
    "test": "node --test tests/*.test.cjs && cd frontend && npm test",
    "preview": "cd frontend && npm run preview",
    "db:apply": "node scripts/apply-schema.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.86.2",
    "dotenv": "^16.4.5",
    "nodemailer": "9.0.3",
    "pg": "^8.13.1"
  }
}
